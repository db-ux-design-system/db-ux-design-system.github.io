---
name: Dependabot review

permissions:
  contents: read
  pull-requests: write

on:
  workflow_call:

jobs:
  review:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-26.04
    steps:
      - name: Prepare dependency review prompt
        uses: mfranzke/dependabot-review-action@ba9c2ed4b62f6c5c13ed316e05549b1d1e96129e # v0.2.1
        with:
          github-token: ${{ github.token }}
