---
import { appConfig } from '@config';
import { DBSection } from '@db-ux/react-core-components';
import { TextImage } from '@components';
---

<DBSection width="medium" spacing="medium">
  <TextImage
    title="Flexible Integration"
    imageSrc={`${appConfig.basePath}assets/for-developer/Flexible-Integration--Lightmode--C.png`}
    imageAlt="Config panel showing integration and customization options"
    imagePosition="left"
    mask="fade-bottom-right"
  >
    Adjust variants and tokens to meet product needs without diverging from the system.
  </TextImage>
</DBSection>
