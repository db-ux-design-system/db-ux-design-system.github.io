---
hidePage: true
category: 'Core'
version: 'v4.5.0'
date: '2026-02-24'
---

### Development

- feat: add `propOverrides` for all components
  - New property to avoid duplicate `id` attributes in Angular and Web Components.
- feat: add new stylelint rule `db-ux/use-sizing`
  - Checks for direct usage of `height`, `width`, `block-size` etc. in the `@db-ux/core-stylelint` package.
- feat: add eslint-plugin
  - New package `@db-ux/core-eslint-plugin` for linting DB UX Design System usage.
- feat: add `wrap` property for possible inline components
