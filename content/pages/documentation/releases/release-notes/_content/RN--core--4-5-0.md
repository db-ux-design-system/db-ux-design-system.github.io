---
hidePage: true
category: 'Core'
version: 'v4.5.0'
date: '2026-02-24'
---

### Development

#### Added
- Components: The new `propOverrides` property avoids duplicate `id` attributes in Angular and Web Components.
- Stylelint: The new rule `db-ux/use-sizing` in the `@db-ux/core-stylelint` package checks for direct usage of `height`, `width`, `block-size` etc.
